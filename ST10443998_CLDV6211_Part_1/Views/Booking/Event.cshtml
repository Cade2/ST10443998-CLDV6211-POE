@model ST10443998_CLDV6211_POE.Models.Event

@{
    ViewData["Title"] = "Event Info";
    var customerId = ViewBag.CustomerId;
}

<h2>Step 2: Enter Event Details</h2>

<form asp-action="Event" method="post">
    <input type="hidden" name="customerId" value="@customerId" />

    <div class="form-group">
        <label asp-for="EventName"></label>
        <input asp-for="EventName" class="form-control" />
    </div>

    <div class="form-group">
        <label asp-for="EventDate"></label>
        <input asp-for="EventDate" class="form-control" type="date" />
    </div>

    <div class="form-group">
        <label asp-for="Description"></label>
        <textarea asp-for="Description" class="form-control"></textarea>
    </div>

    <div class="form-group">
        <label for="VenueId">Venue</label>
        <select asp-for="VenueId" class="form-control" asp-items="@(new SelectList(ViewBag.Venues, "VenueId", "VenueName"))">
            <option value="">-- Select Venue --</option>
        </select>
    </div>

    <div class="form-group">
        <label for="EventTypeId">Event Type</label>
        <select asp-for="EventTypeId" class="form-control" asp-items="@(new SelectList(ViewBag.EventTypes, "EventTypeId", "TypeName"))">
            <option value="">-- Select Event Type --</option>
        </select>
    </div>

    <br />
    <button type="submit" class="btn btn-primary">Continue to Payment</button>
</form>
